<%= form_for [:admin, route], html: { style: "display: none" } do |f| %>
  <% if route.errors.any? %>
    <div id="error_explanation">
      <h2><%= t('common.errors') %></h2>

      <ul>
      <% route.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, class: 'sr-only' %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit t('common.update') %>
  </div>
<% end %>
