<h3><%= t('.title') %></h3>

<table class='table table-hover'>
  <thead>
    <tr>
      <th><%= t('.number') %></th>
      <th><%= t('.route') %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%= render @trains %>
  </tbody>
</table>

<br>

<%= link_to t('.new_train'), new_admin_train_path, class: 'btn btn-primary' %>
