<h1><%= t('.title') %></h1>

<%= render 'form', train: @train %>

<%= link_to t('common.show'), admin_train_path(@train), class: 'btn btn-primary' %> |
<%= link_to t('common.back'), admin_trains_path, class: 'btn btn-primary' %>
