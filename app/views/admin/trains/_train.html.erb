<tr>
  <td><%= train.number %></td>
  <td><%= train.route.present? ? link_to(train.route.name, admin_route_path(train.route)) : 'Маршрут не назначен' %></td>
  <td><%= link_to 'Посмотреть', admin_train_path(train) %></td>
  <td><%= link_to 'Редактировать', edit_admin_train_path(train) %></td>
  <td><%= link_to 'Удалить', admin_train_path(train), method: :delete, data: { confirm: 'Вы уверены?' } %></td>
</tr>
